<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="270dp"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:paddingLeft="5dp"
    android:paddingRight="5dp">

    <ImageView
        android:id="@+id/drawer_item_icon_imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/drawer_item_icon"
        android:paddingLeft="16dp"
        android:paddingRight="0dp"
        android:paddingStart="16dp"
        android:paddingEnd="0dp"/>

    <TextView
        android:id="@+id/drawer_list_item_title"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:layout_marginLeft="32dp"
        android:layout_marginStart="32dp"
        android:layout_toEndOf="@+id/drawer_item_icon_imageView"
        android:layout_toRightOf="@+id/drawer_item_icon_imageView"
        android:gravity="center_vertical"
        android:textColor="@color/primary_dark_material_light"
        android:textSize="18sp" />

</RelativeLayout>